spring:
  application:
    name: category-service

  datasource:
    url: jdbc:mysql://localhost:3306/category_service_db
    username: root
    password: Kvishwash@2001
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 5003

eureka:
  instance:
    prefer-ip-address: true

  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka/